{
  "name": "Ven0m0 .github",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "bash -lc 'if command -v mise >/dev/null 2>&1; then mise install; else printf \"%s\\n\" \"Install mise in the container, then run: mise install\"; fi'",
  "customizations": {
    "vscode": {
      "settings": {
        "files.eol": "\n"
      }
    }
  }
}
